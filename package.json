{
  "name": "tuidaw",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run index.ts",
    "check": "tsc --noEmit && tsc --noEmit -p web/tsconfig.json",
    "test": "bun test",
    "install": "bash setup.sh && bash native/build-wasm.sh",
    "build": "bun run check && bun test && bash native/build.sh && bash native/build-wasm.sh"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@opentui/core": "^0.1.88"
  }
}
